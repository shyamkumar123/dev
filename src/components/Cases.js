import React from 'react';
import styled from 'styled-components';

const Container = styled.div`
  padding: 20px;
`;

const Cases = () => {
  return (
    <Container>
      <h2>Investigation Cases</h2>
      <p>List of investigation cases will be displayed here.</p>
    </Container>
  );
};

export default Cases;
